// Add two arrays of equal dimensions.

real add_real_arrays(real[] x, real[] y);

real add_real_arrays(real[] x, real[] y) {

  int n_x = dim(x);
  int n_y = dim(y);

  if(num_elements(n_x) == 1) {
    for
    xpy[]
  }


  return xpy;
}
